<%= turbo_stream.remove "request-#{request_id}" %>
<%= turbo_stream.replace "user-follow-form-#{user.id}" do %>
    <%= render partial: "users/form", locals: {current_user: current_user, user: user }  %>
<% end %>

