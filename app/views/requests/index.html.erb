<%= turbo_stream_from "requests-#{current_user.id}" %>
<div id="requests-<%= current_user.id %>">
    <%= render @requests%>
</div>
<% if @pagy.next.present? %>
    <%= turbo_frame_tag :next_page, src: requests_path(page: @pagy.next, format: :turbo_stream), loading: :lazy %>
<% end %>
