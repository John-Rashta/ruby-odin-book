<% if @comment.comment_id.is_a?(Numeric) %>
    <%= turbo_stream.prepend "comment-show-comments-#{@comment.comment_id}", @comment %>
    <%= turbo_stream.append "comment-comments-#{@comment.comment_id}", @comment %>
    <%= turbo_stream.update "comments-comment-number-#{@comment.comment_id}" do %>
        <%= @comment.comment.comments_count %>
    <% end %>
<% else %>
    <%= turbo_stream.prepend "post-comments-#{@comment.post_id}", @comment %>
<% end %>
<%= turbo_stream.update "comments-post-number-#{@comment.post_id}" do %>
    <%= @comment.post.comments_count %>
<% end %>
